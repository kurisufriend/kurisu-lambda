: get it? because std?
: i'm hilarious.

(defun spit
	(id miracle
		"builtins" "print" (id $1))
)
(defun input
	(id miracle
		"builtins" "input" (""))
)
(defun thesis
	(id spit
		("this language belongs to makise kurisu. there are many like it, but this one is hers."))
)